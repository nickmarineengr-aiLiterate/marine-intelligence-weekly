# QP2512 Q8 — verification record

**Paper:** QP2512, December 2025. **Authored:** 2026-08-12. **State:** STAGED at `staging/QP2512/Q8.json`.
**Reuse:** Tier C. **Composite limb-level support, authored fresh.** `QP2508-Q5` (limb 1) and
`QP2606-Q2` (limbs 2–3, structure only). No whole-question donor.

> **STAGING NOTE.** Staged; not promoted into `specs/QP2512.json`.

**Question.** Define an 'Unseaworthy ship' within the meaning of MS Act. Explain 'detainable
deficiency' with reference to a PSC inspection and describe the procedure to be followed for timely
release of a vessel detained for serious structural deficiencies under PSC. **(16)**

## Source verification

Printed stem read from `DECEMBER  - 2025.pdf` (sha256 `dd651a5f…`, 234,066 bytes — hash re-verified
this session against the checkpoint). Three tasks under a single printed `(16)`, no sub-part
allocation. The typographic single quotes around *'Unseaworthy ship'* and *'detainable deficiency'*
are as printed and are reproduced.

## Preferred donor and why there is none

| Candidate | Sitting | Direction | Ruling |
|---|---|---|---|
| `QP2508-Q5` | August 2025 | forward, 4 months | **limb 1 only** — reused as verified internal reuse |
| `QP2506-Q9`, `QP2602-Q5` | — | — | same family as `QP2508-Q5`, no additional coverage |
| `QP2606-Q2` | June 2026 | **backward, 6 months** | **structure only, no text carried** — see below |

Neither donor sets this examiner's task. `QP2508-Q5` additionally asks the unseaworthy/unsafe
distinction and the owner's obligations to crew, neither of which this stem sets. `QP2606-Q2` asks
for the legal framework of PSC and the right of appeal in two marked 8-mark limbs; this stem asks
for detainable deficiency and the release procedure under one printed `(16)`. **Tier C with limb
support, not tier D.**

### Why `QP2606-Q2` was NOT cloned — the decision that protects this question

`QP2606-Q2` is built **entirely on `A.1206(34)`**, the 2025 edition of the Procedures, which did not
exist at this sitting. The harness would have let it be cloned and patched. It was not, and the
reason is arithmetic rather than caution: the intake temporal note predicts an instrument asserted
across **eight separate surfaces** of a single question object — model answer, study guide,
`recall_15s`, `major_trap`, a route core point, a retrieval card, `regulations` and
`search_aliases`. Cloning would have meant finding and reversing the excluded edition on all eight.
**Authoring fresh means it can never enter.** Its section structure informed the shape of the
answer; none of its text was carried.

## The three deltas

| Delta | Finding |
|---|---|
| **Question** | **Substantial.** No donor stem matches. Limb 1 is a subset of `QP2508-Q5`; limbs 2–3 overlap `QP2606-Q2` in subject but differ in demand — *detainable deficiency* and *timely release* against *legal framework* and *right of appeal*. |
| **Marks** | `QP2508-Q5` printed `(16)` for a three-limb question; `QP2606-Q2` printed `8 + 8`. This stem prints a single `(16)` across three unequal tasks. Depth re-derived from this paper's own printing. |
| **Temporal** | **The largest on the paper.** One statute boundary and one instrument-edition boundary, in opposite directions. Both resolved below. |

## Temporal review — December 2025

### The Indian statute boundary — answered, flag was real

The intake flag is `INDIAN STATUTE BOUNDARY` at `HIGH`. It is a genuine risk and it resolves
cleanly.

```
Merchant Shipping Act, 2025    assent        18 August 2025
                               COMMENCEMENT  15 March 2026    <-- after this sitting
Merchant Shipping Act, 1958    OPERATIVE at December 2025, in full
```

The model answer is written **wholly on the 1958 Act** and **does not name the 2025 Act anywhere**.
The 15 March 2026 commencement appears once, in the study guide's uncertainty section, explicitly
labelled as a change occurring after this sitting — the treatment
`QP2512_TEMPORAL_AND_DONOR_ANCHOR.md` §7 authorises for legitimate future context. It is absent
from the route, the cards, `quick_revision`, `regulations` and `search_aliases`.

**The premise was checked separately from the conclusion.** `WORKFLOW_LESSONS.md` *"A right
conclusion reached through a PREMATURE premise"* is live on exactly this pair. `QP2508-Q5` reaches
the 1958 Act as the law in force **and reasons from the 2025 Act not having commenced**. Right
conclusion, right premise. The reuse was accepted on that basis, not on the conclusion matching.

### The PSC edition — the real trap, avoided by construction

Three editions exist across this corpus, and a two-edition mental model gets this wrong:

| Edition | Resolution | Adopted | Revokes |
|---|---|---|---|
| Procedures for PSC, 2021 | `A.1155(32)` | 15 December 2021 | `A.1138(31)` and predecessors |
| **Procedures for PSC, 2023** | **`A.1185(33)`** | **6 December 2023** | `A.1155(32)` |
| Procedures for PSC, 2025 | 34th Assembly | 3 December 2025 | `A.1185(33)` |

**`A.1185(33)` is the operative edition at a December 2025 sitting.** Reaching backwards to
`A.1155(32)` skips an entire edition; inheriting the 2025 edition from the June 2026 donor imports
an instrument that had not been issued. **No `A.12xx(34)` resolution appears in any field of this
object.**

`A.1185(33)` was **re-obtained from the IMO resolutions CDN and read at source this session** — 167
pages, `A 33/Res.1185`, adopted 6 December 2023, reissued 5 March 2024 with editorial changes.
Operative paragraph 4 (*"REVOKES resolution A.1155(32)"*) was read directly, confirming the chain.
The PDF was held in session scratch and **not committed**, per `PASTPAPER_PRODUCTION_PROTOCOL.md`
§2.

### Paragraph numbering — verified, not assumed

The anchor warned that chapter numbering moves between editions and that the equivalents must be
located directly. Done, with one notable result:

- **The right of appeal is at `2.3.11` in `A.1185(33)` as well.** On this provision the numbering
  **did** carry across to the 2025 edition. That is a **negative finding established by reading**,
  not an assumption — and it is worth recording precisely because the warning predicted it might
  not.
- Every other paragraph cited was located in the 2023 text directly: `1.7.3`, `1.7.4`, `1.7.7`,
  `1.7.11`, `2.3.11`, `3.5.2`, `3.6`, `3.7.1`–`3.7.6`, `4.1.3`, `4.1.5`, `4.2.1`, and appendix 2
  §§1, 3.1, 3.3, 3.4, 4, 5.

### The 34th Assembly boundary

Engaged and excluded. `A 34/Res.1206` was adopted 3 December 2025 and **issued 5 December 2025**;
the paper prints only `DECEMBER 2025` with no day-level date. No resolution of the 34th session is
cited anywhere in this question.

## Internal `Q`-reference sweep

Run over every string in the object. Because the question was authored rather than cloned, **no
donor `Q`-reference could be inherited** — the class that fired on Q2 cannot arise here. Two
internal references were **authored deliberately** and both were adjudicated against QP2512:

| Reference | Adjudication |
|---|---|
| `cross_links[0]` → `QP2512.html#q1` | **CORRECT.** Q1 on this paper is the ship-health-certificate question and does turn on which Indian instrument was in force. **This closes the reciprocal of Q1's staged link to `#q8`**, which was the dependency the checkpoint recorded as intentionally unresolved. |
| study guide *"Read this question against **Q1 on this paper**"* | **CORRECT**, same basis. Names the paper explicitly. |
| `cross_links[1]` → `QP2508.html#q5` | **CORRECT.** Cross-paper, names its paper, resolves. |

## Corpus use

**None.** The paper's instruments here are the Merchant Shipping Act 1958, `A.1185(33)` and SOLAS —
none of which is among the three True Source corpora. `reference_shelf` is **empty, which is the
correct outcome and not an omission**.

Private corpus verified this session at `64977b86ed9c601e273f1d0cb55abb0461835811`, matching the
checkpoint. Upstream `origin/main` is **5 commits ahead**, all of it FSS Code / MARPOL Annex VI
text-layer and rights work (`FD-RIGHTS-2`, `FD-RIGHTS-3`). **None of it touches any instrument this
paper cites**, so the checkpoint's finding stands unchanged and the corpus was not pulled.

## Primary-source use

| Source | Use |
|---|---|
| **`A.1185(33)`, read at source** | definitions 1.7.3/1.7.4/1.7.7/1.7.11; 2.3.11 appeal; 3.5.2 notification; 3.6 suspension; 3.7.1–3.7.6 rectification and release; 4.1.3 notification content; 4.2.1 GISIS; appendix 2 in full |
| **MS Act 1958 ss.334, 335, 336** | verified internal reuse from the `QP2508-Q5` record, which read the sections at source |
| **SOLAS I/19, XI-1/2** | convention basis of control; enhanced survey programme |
| **MARPOL article 7(2)** | undue delay and compensation — named, not quoted |

The eleven capability tests in appendix 2 §3.4 and the §3.7.3 repair-yard conditions are quoted
**from the resolution text itself**, not paraphrased from a donor.

## What was reused, what was rewritten

**Reused (verified internal reuse):** the section 334 heads and the `s.334(1)`/`(2)` offence
structure from `QP2508-Q5`.

**Authored fresh:** everything else — the entire PSC treatment, the detainable-deficiency concept,
the eleven capability tests, both branches of the release procedure, the route, the study guide, the
recall layer and all ten retrieval cards.

**Re-asserted from intake, last:** `q_no`, `anchor`, `text_verbatim`, `subparts`, `total_marks`,
`question_id`, `host_recurrence_hint`.

## Remaining limitations

1. **The 2011 ESP Code** is named as the regime the enhanced survey programme runs under. MIW holds
   no licensed copy; the naming rests on SOLAS XI-1/2, which is held and makes the Code mandatory.
   No clause is quoted. Class `C_ACCEPTED_LIMITATION`.
2. **Regional MoU practice** (Indian Ocean, Paris, Tokyo) layers its own inspection,
   detention-publication and banning regime over the IMO Procedures. Those texts are not IMO
   instruments and are not held; the layer is **named rather than detailed**.
3. **No time limit for release is asserted**, because the Procedures set none. How quickly release
   follows is a function of the port, the regional MoU and the repair. `PASTPAPER_PRODUCTION_
   PROTOCOL.md` §2.1 applied — an unverifiable quantity is omitted, not invented.
4. **`A.1185(33)` currency is flagged `B_CURRENCY_CHECK`** — correct for this sitting and fixed by
   it, but any future session reusing this answer for a *later* paper must re-derive the edition
   rather than inherit it.

## Word count

Model answer **1,491 words**. Outside the deferred 450–650 band, which is a known open item
(`CURRENT_STATUS.md` §6 item 3). Measured against the corpus rather than the band: across **117
built 16-mark answers** the median is **1,038** and the mean **1,172**, with a maximum of 2,709.
This question sits between the median and the 90th percentile — **inside house norm, not an
outlier** — and it is the paper's only question setting three tasks across two legal systems. Not
trimmed.

## Verdict

**READY** — subject to the paper as a whole being completed, built and swept.
