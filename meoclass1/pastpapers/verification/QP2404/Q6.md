# QP2404 Q6 - verification record

**Paper:** QP2404, April 2024, MEO Class I
**Question:** Q6, 16 marks, printed 8 + 8 - general average and refloating damage
**Reuse:** Tier **D**, `reused_from` = **`QP2602-Q6`**. **Status: READY.**
**Authored:** 2026-08-10, restored from staging 2026-08-11.

> **THE GUARDED FIELD.** `reused_from` on this question is **`QP2602-Q6`** and must not be
> "tidied" to the structural donor. See section 2 - this is the one trap the staging checkpoint
> carried, and the guarded resume preserved it.

---

## 1. Donors

| Object | Sitting | Role |
|---|---|---|
| `QP2506-Q6` | June 2025 | **structural donor** - printed stem raw-identical; the answer was adapted from this object |
| `QP2508-Q6` | August 2025 | second identical donor, consulted; the set is deliberately kept identical |
| **`QP2602-Q6`** | February 2026 | **third donor, added by reverse-hint adjudication**; the value written to `reused_from` |

**Direction:** April 2024 is the **earliest** sitting in the family. Every donor is a later answer
pulled backwards.

## 2. The guarded field - why `reused_from` is `QP2602-Q6`

`QP2506-Q6` and `QP2508-Q6` join the general-average family **on their own**, by **exact stem
equality**. `QP2602-Q6` does **not**: its stem differs by a **single inserted word** - "give
**proper** justification" - so no exact-equality edge exists and the **explicit `reused_from` edge
is the only thing holding it in the family**.

**Replacing it with the structural donor would drop `QP2602-Q6` out of the family and regress an
already-built page back to "Once in this set".** The staging checkpoint carried this warning, the
authoring script carries it on the line itself, and the guarded resume was verified this session
to preserve it - confirmed after restore.

In this schema `reused_from` is an **undirected recurrence family edge**, not an answer-donor
pointer, which is what makes this correct rather than a misstatement of provenance. The answer
donor is recorded in `reuse_evidence`.

## 3. The three deltas

| Delta | Finding |
|---|---|
| **Question** | Printed stem **IDENTICAL** to `QP2506-Q6`, word for word including the marks. |
| **Marks** | **NIL** - 8 + 8. |
| **Temporal** | **NIL.** The York-Antwerp Rules 2016 were the current CMI text at April 2024, and Rule VII is materially unchanged from the 1994 and 2004 versions, so an older incorporated version would not alter the answer. |

**What changed is the SURROUNDINGS** - see section 4.

## 4. PIL findings - the finding that changed the answer

`temporal_sweep.py` was run **prospectively, over the donors before adaptation**, and flagged
`INTERNAL_QREF` on both `QP2506-Q6` ("See Q5 of this paper") and `QP2508-Q6` ("See Q1 of this
paper").

**Adjudication: REAL RISK. The cross-reference is DROPPED, not renumbered.** The donors point at
their own papers' **LLMC limitation** question. **This paper has no LLMC question** - on QP2404,
Q5 is antifouling paint and Q1 is IoT. Mechanically renumbering the pointer would have sent a
candidate to an unrelated question; copying it unchanged would have done the same.

This is the worked example of the session lesson: *if a donor says "See Q5 of this paper" and the
target paper's Q5 is unrelated, drop the pointer.*

A sweep of the completed paper confirms **zero** candidate-facing internal Q-references anywhere in
QP2404. That zero is controlled - the sweep's `--retrospective` case fires on exactly this pattern
and passes.

`temporal_sweep.py` raised no post-sitting date or year candidate on this question.

## 5. Source verification

| Source | Class |
|---|---|
| **York-Antwerp Rules 2016**, CMI text - **read**. Rule of Interpretation, Rule Paramount, Rules A to E, and Rule VII | **P1** |
| Indian Marine Insurance Act 1963 - the general average definition, **referred to by name only** | see section 6 |

Provenance summary carried: 26 P1, 4 P2, 1 internal reuse verified, 3 engineering judgement.

## 6. What could not be verified

- **The section of the Indian Marine Insurance Act 1963 defining a general average loss was not
  read, and no section number is asserted anywhere.** The statutory definition is referred to by
  name only. A future session holding the Act should supply the section.
- **Which version of the York-Antwerp Rules applies to any given adventure is a matter of the
  contract of carriage, not of law.** The answer quotes the 2016 text and records that Rule VII is
  materially identical in the 1994 and 2004 versions.

## 7. Reverse-hint adjudication

| | |
|---|---|
| Pair | `QP2404-Q6` <- `QP2602-Q6` |
| Normalised stem difference | **one inserted word**: `proper` |
| MIW ruling | **EXACT - the same examiner task** |
| Consequence | Already Tier D; adds a **third** donor, and corrects `QP2602-Q6`, which until now rendered **"Once in this set"** |

**Accepted from the prior session and not re-researched.** Note the derived public-surface effect:
this edge changes an already-built, already-approved page. That is reported under surface impact,
not hidden.

## 8. Verdict

**READY.** The York-Antwerp Rules were read at source in the donor object, all three deltas are
nil and recorded, the guarded `reused_from` edge survived the resume intact and was verified after
restore, and the donor's internal cross-reference was dropped rather than mechanically renumbered.
